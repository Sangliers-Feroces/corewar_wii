/*
** EPITECH PROJECT, 2018
** __xd__world
** File description:
** swaggy demo
*/

#include "headers.h"

void material_fun_wood_entity(dmat4 mvp, dmat4 world, dmat4 rot)
{
    (void)mvp;
    (void)world;
    (void)rot;
    /*shader_set(SHADER_WORLD, mvp, world, rot);
    glActiveTexture(GL_TEXTURE0);
    glBindTexture(GL_TEXTURE_2D, _demo->tex[TEX_WOOD]->id);*/
}

void material_fun_npc1_entity(dmat4 mvp, dmat4 world, dmat4 rot)
{
    (void)mvp;
    (void)world;
    (void)rot;
    /*shader_set(SHADER_WORLD, mvp, world, rot);
    glActiveTexture(GL_TEXTURE0);
    glBindTexture(GL_TEXTURE_2D, _demo->tex[TEX_NPC1]->id);*/
}

void material_fun_metal_rust_entity(dmat4 mvp, dmat4 world, dmat4 rot)
{
    (void)mvp;
    (void)world;
    (void)rot;
    /*shader_set(SHADER_WORLD, mvp, world, rot);
    glActiveTexture(GL_TEXTURE0);
    glBindTexture(GL_TEXTURE_2D, _demo->tex[TEX_METAL_RUST]->id);*/
}

void material_fun_metal_bare_entity(dmat4 mvp, dmat4 world, dmat4 rot)
{
    (void)mvp;
    (void)world;
    (void)rot;
    /*shader_set(SHADER_WORLD, mvp, world, rot);
    glActiveTexture(GL_TEXTURE0);
    glBindTexture(GL_TEXTURE_2D, _demo->tex[TEX_METAL_BARE]->id);*/
}
